import React from 'react';

const Fixtures = () => {
  return (
    <div
      className="container d-flex flex-wrap card text-center mx-auto shadow p-3 mb-5 rounded"
      style={{ width: '50rem' }}
    >
      <h1>Fixtures</h1>
    </div>
  );
};

export default Fixtures;
